"removeComments": false, ->true
//src\components\_ComponentsLink\AjaxLogic.ts   ---- надо тестануть использование await
//дописал src\components\Body\CardsList\BodyCardsListMain.tsx но без async, тестануть сначала так, а потом то что выше попробовать
//метод редактирования должен возвращать новый объект, надо все под это переписать
//удаление картинки
//картинки фул не работают и фронт и бэк

карусель id-4
кнопки влево вправо селать видными(если фон белый то их не видно вообще)
в идеале зафиксировать высоту?
редач
в урл добавляется #carouselExampleControls , надо бы поправить

!картинки
//сейчас их при хранении в файловой системе надо сохранять так /images/uploads... а не так uploads...
//и соотверственно при рендере надо убрать использование G_PathToBaseImages
//не нужна--//надо подумать нужна ли G_PathToBaseImages тк я везде могу просто сохранять с /path а вот если будет внешний источник картинок то будут проблемы тк там будет фул сайт а я туда прилеплю что то еще


filename обязательно надо генерить свой!
maincomponent - componentDidMount
доделать алерты
редактирование детальки
валидация кратинок
удаление карточки


добавление\удаление алертов надо сделать под каким то lock?
сохранить макс id, и его увеличивать, при удалении алерта проверять пустой ли список, если пустой то макс =0

алерты должны жить не долго, закрываться через секунд 20
на странице карточки сделать доп картинки
если с бека вернулась ошибка но не в формате resp.errors ->[] а там объект(прям сейчас возвращается при ошибке modelState то это тоже надо обработать


в хедере длинное имя как то поправить или вообще мб его выпилить
при экране 560 пикселей хорошо что бы карточка не занимала фул экран
дебаг не работает
файл конфига дебага - launch.json
ESLint - ?
поменять везде обновление state под функциональный стиль, и вообще почитать надо ли



css bundle @webpack combine all css files@
https://code.luasoftware.com/tutorials/webpack/merge-multiple-css-into-single-file/#:~:text=The%20best%20way%20to%20merge,%2Dtext%2Dwebpack%2Dplugin.&text=Edit%20webpack.,to%20include%20setup%20the%20plugin.











